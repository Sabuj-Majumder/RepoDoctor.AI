import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Navbar } from "@/components/navbar";
import { Footer } from "@/components/footer";
import { TooltipProvider } from "@/components/ui/tooltip";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "RepoDoctor AI - GitHub Repository Health Analyzer",
  description: "Analyze your GitHub repositories for code quality, documentation, activity, and best practices. Get AI-powered recommendations to improve your open source projects.",
  keywords: ["GitHub", "repository", "analysis", "code quality", "open source", "AI"],
  authors: [{ name: "RepoDoctor AI" }],
  openGraph: {
    title: "RepoDoctor AI - GitHub Repository Health Analyzer",
    description: "Analyze your GitHub repositories for code quality, documentation, activity, and best practices.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased dark`}
    >
      <body className="min-h-full flex flex-col bg-black text-white">
        <TooltipProvider>
          <Navbar />
          <main className="flex-1">{children}</main>
          <Footer />
        </TooltipProvider>
      </body>
    </html>
  );
}
